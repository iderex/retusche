<!--
Everything about this change is argued here, in the body. A comment underneath
is not where a change is argued, and a body that has gone out of date is edited
rather than corrected below it.

Delete a heading only if you have replaced it with something better, not
because it was inconvenient to fill in.
-->

Closes #

## What failure this prevents

<!-- Not what the change does. What goes wrong without it, and for whom. -->

## The means

<!--
Which language, format, tool or runtime this is built out of, and why that fits
here. Answer in this order:

- can the means carry a property a machine refuses, a proof that runs, and a
  claim that cites the command behind it
- is anything outside this repository forcing it, and is that force named
- does it add a language, a runtime or a dependency this tree does not already
  carry, and is that cost paid knowingly
- is the result testable by the suite that exists, or does it need a parallel
  apparatus nobody will maintain

Carried over from the last change is not an answer.
-->

## Evidence

<!--
Every asserted fact carries the command that produced it, run at the commit
being pushed. Paste the command and its output, not the conclusion.

A guard is proven by making it fail on purpose: show the tree red for the reason
the guard names, show it green again, and say that the fixture was removed. A
near-miss that could not have failed proves less than one that nearly did.
-->

## What this does not do

<!--
The limits of what was measured. Anything skipped, anything not evaluated on
this route, anything a reader might otherwise read as covered. This section
never gets rewritten into a reassurance.
-->

## Documentation

<!--
Which documents this change makes wrong, and whether they are updated here. If
nothing goes out of date, say that.
-->

## Review

<!-- Who has read this besides its author. If nobody has, write that. -->
